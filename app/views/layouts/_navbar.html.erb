<nav class="navbar navbar-inverse">
  <div class="container">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <a class="navbar-brand" href="/">Flow<strong>Overstack</strong></a>
    </div>

    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
      <ul class="nav navbar-nav navbar-right">
          <%= search_form_for(@search, url: questions_path, html: { class: 'navbar-form navbar-left' }) do |f| %>
            <div class="form-group"> <%= f.search_field :title_or_content_cont, class: 'form-control', placeholder: 'Search' %></div>
            <div class="actions">
              <%= button_tag type: 'submit', class: "glyphicon glyphicon-search" do %>
              <% end %>
            </div>
          <% end %>
        <% if user_signed_in? %>
          <li><%= link_to '', new_question_path, class:'fa fa-plus' %></li>
          <li class="dropdown">
            <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false"><%= current_user.email.split('@')[0].upcase %><span class="caret"></span></a>
            <ul class="dropdown-menu">
              <li><%= link_to 'New Question',new_question_path %></li>
              <li> <%= link_to 'Log Out', destroy_user_session_path, method: :delete %> </li>
            </ul>
          </li>
        <% else %>
          <li> <%= link_to 'Log In', new_user_session_path %> </li>
          <li> <%= link_to 'Sign Up', new_user_registration_path %> </li>
        <% end %>
      </ul>
    </div><!-- /.navbar-collapse -->
  </div><!-- /.container-fluid -->
</nav>
